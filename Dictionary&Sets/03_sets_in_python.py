''' properties of set
    1) sets are unorded
    2) sets are unindexed
    3) there is no way to change the items of sets
    4) sets can not contain duplicate values '''
x={3,76,23,90,76}
print(type(x)) # sets does not hold repetitive elements
print(x)

# This syntex will create dictionary not a empty set
y={ }
print(type(y))

# an empty set can be created by this syntex
z= set ()
print(type(z))


